"""Shared layout width for Streamlit charts and wide dataframes (Altair, tables).

Import `ST_WIDTH` in `pages/*.py` so graphics stay consistent with the wide layout.
"""

from __future__ import annotations

ST_WIDTH: str = "stretch"
